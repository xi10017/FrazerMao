---
description: Build the MuPractice project
---

# Build Project

Follow these steps to install dependencies and build the MuPractice project for production.

1.  **Install Dependencies**:
    ```bash
    npm install
    ```

2.  **Build Project**:
    ```bash
    npm run build
    ```
