{
  "entities": {
    "TestSubmission": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "TestSubmission",
      "type": "object",
      "description": "Represents a user's score on a particular test.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Reference to the User entity."
        },
        "testId": {
          "type": "string",
          "description": "Reference to the Test entity."
        },
        "score": {
          "type": "object",
          "properties": {
            "totalScore": {"type": "number"},
            "correctCount": {"type": "number"},
            "incorrectCount": {"type": "number"},
            "omitCount": {"type": "number"}
          }
        },
         "answers": {
          "type": "object",
          "description": "A map of question numbers to user answers."
        },
        "submittedAt": {
          "type": "string",
          "description": "The timestamp when the test was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "testId",
        "score",
        "answers",
        "submittedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
     "structure": [
      {
        "path": "/users/{userId}/submissions/{submissionId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/TestSubmission"
          },
          "description": "Stores user scores for each test. Path-based ownership ensures only the user can access their scores."
        }
      }
    ]
  }
}
